spring:
  application:
    name: auth-server
  cloud:
    config:
      uri: http://localhost:8888
      name: auth-server
      profile: dev
  eureka:
    client:
      service-url:
        defaultZone: http://eureka-server:8761/eureka/